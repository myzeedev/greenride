The project is still in development and not fully completed, 
as I only started earlier today. Due to other tasks, 
I was unable to finish it. However, 
the current version is functional and can be tested.

Clone the GreenRide, run npm install, to install all dependencies
Then start the app with -> npx expo run:android or npx expo run:ios
